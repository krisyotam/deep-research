image: alpine/edge
secrets:
  - stargate_ssh
tasks:
  - deploy: |
      ssh -o StrictHostKeyChecking=no krisyotam@stargate.taild7f15a.ts.net '
        set -eu
        cd ~/deep-research
        git fetch --quiet origin
        git reset --hard origin/master
        npm ci --silent
        npm run build
        echo "deep-research rebuilt"
      '
